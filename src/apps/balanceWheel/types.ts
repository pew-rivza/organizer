export interface Wheel {
  id?: number,
  date?: string,
}

export interface Area {
  id: number,
  name: string,
  icon: string,
}

export interface AreaValue {
  id: number,
  value: number,
  BWAreaId: number,
}
